# AWS TGSAIOps Dashboard - Environment Variables Example
# Copy this file to .env.local and replace the placeholder values with your actual AWS credentials

# Default AWS credentials
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_REGION=us-east-1

# Environment-specific AWS credentials (optional)
# These will be used when the corresponding environment is selected in the dashboard

# Development environment
AWS_ACCESS_KEY_ID_DEV=your_dev_access_key
AWS_SECRET_ACCESS_KEY_DEV=your_dev_secret_key
AWS_REGION_DEV=us-east-1

# UAT environment
AWS_ACCESS_KEY_ID_UAT=your_uat_access_key
AWS_SECRET_ACCESS_KEY_UAT=your_uat_secret_key
AWS_REGION_UAT=us-east-1

# Production environment
AWS_ACCESS_KEY_ID_PROD=your_prod_access_key
AWS_SECRET_ACCESS_KEY_PROD=your_prod_secret_key
AWS_REGION_PROD=us-east-1

# Note: If environment-specific credentials are not provided,
# the dashboard will fall back to using the default credentials.
# If no credentials are provided at all, the dashboard will use mock data.
